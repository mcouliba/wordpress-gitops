apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wordpress-gitops-prod
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/instance: wordpress-gitops-prod
spec:
  destination:
    namespace: openshift-gitops
    server: 'https://kubernetes.default.svc'
  project: default
  source:
    path: .
    repoURL: 'https://github.com/mcouliba/wordpress-gitops'
    targetRevision: 'prod'
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true